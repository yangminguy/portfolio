# Archive

This folder keeps old files out of the production path.

Use `v2-deck/` as the only active app.
Do not use archived files as implementation references unless a human explicitly asks for a historical comparison.
